# Copy this file to frontend/.env when the backend is not at the default URL.
# All VITE_* vars are inlined into the frontend bundle at build time.
VITE_API_URL=http://localhost:5000
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
